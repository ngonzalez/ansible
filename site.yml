---
  # docker
- hosts: localhost
  roles:
    - docker

  # requirements
- hosts: sid.home:stretch.home
  become: True
  gather_facts: False
  roles:
    - python
    - localtime
    - motd
    - locales
    - git
    - vim
    - zsh
    - elastic_co
    - filebeat
    - rsyslog

  # elasticsearch
- hosts: stretch.home
  become: True
  gather_facts: False
  tags: elasticsearch
  roles:
    - elasticsearch
    - logstash
    - kibana

  # databases
- hosts: sid.home
  become: True
  gather_facts: False
  tags: databases
  roles:
    - redis
    - postgresql
    - jetty

  # application
- hosts: sid.home
  become: True
  gather_facts: True
  tags: deploy
  roles:
    - app

  # nginx
- hosts: sid.home:stretch.home
  become: True
  gather_facts: False
  tags: nginx
  roles:
    - nginx
