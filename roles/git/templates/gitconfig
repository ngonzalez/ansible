[core]
  editor = vim
  whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
[color]
  ui = auto
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = "#f97298 bold"
  new = "#8bf972 bold"
  whitespace = red reverse
[color "diff-highlight"]
  oldNormal = "black #f8cbcb"
  oldHighlight = "black #f97298"
  newNormal = "black #cbeecb"
  newHighlight = "black #8bf972"
[color "status"]
  added = yellow
  changed = green
  untracked = cyan
[alias]
  st = status
  ci = commit
  br = branch
  co = checkout
  df = diff
  dc = diff --cached
  lg = log -p
  lol = log --graph --decorate --pretty=oneline --abbrev-commit
  lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
  ls = ls-files
  ign = ls-files -o -i --exclude-standard
[user]
  email = nicolas.gonzalez@trainline.com
  name = Nicolas Gonzalez
[url "git@github.com:"]
  insteadOf = https://github.com/