---
- name: Install redis-server
  apt:
    name: redis-server

- name: Configure redis
  template:
    src: redis.conf.j2
    dest: "{{ redis_config_file }}"
    mode: 0644

- name: Start redis
  systemd:
    daemon_reload: yes
    state: started
    name: redis
